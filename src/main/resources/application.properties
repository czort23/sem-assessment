# ==================================================
# DATABASE CONFIGURATION
# ==================================================
db.driver=com.mysql.cj.jdbc.Driver

# ? Use Docker network service name for GitHub Actions and local docker-compose
db.url=jdbc:mysql://db:3306/world?allowPublicKeyRetrieval=true&useSSL=false

db.username=root
db.password=example

# Retry connection attempts (useful when MySQL starts slowly)
db.connect.retries=5
db.connect.delay=10000