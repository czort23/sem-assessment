# ==================================================
# DATABASE CONFIGURATION
# ==================================================
db.driver=com.mysql.cj.jdbc.Driver

# Use this when running tests from IntelliJ or Maven on your host
db.url=jdbc:mysql://localhost:33060/world?allowPublicKeyRetrieval=true&useSSL=false

# Use this instead only when the app runs inside Docker
# db.url=jdbc:mysql://db:3306/world?allowPublicKeyRetrieval=true&useSSL=false

db.username=root
db.password=example

db.connect.retries=5
db.connect.delay=10000